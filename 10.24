
1010 一元多项式求导 （25 分）

def function(x,y):
    if y==0:
        return []
    else:
        x=x*y
        y=y+1
        return [x,y]
n = input().split()
a=list(map(int,n))
numbers=[]
for i in range(0,len(a),2):
    b = function(a[i],a[i+1])
    numbers.extend(n)
for j in range(len(numbers)):
    if j == len(numbers)-1:
        print(numbers[j],end='')
    else:
        print(numbers[j],end=' ')
        
不知道为什么无法解决，希望有大佬能帮忙        
